

######## DATA TOOLBOX ############

git config --global use

d:
cd "D:/Mis Documentos/GitHub"

git ls-tree --full-tree -r HEAD


cd
mkdir test-repo
cd test-repo
git init

## Add Remote Repositories
$ git remote add [shortname] [url]
git remote add origin https://github.com/ronaldrobles/test-repo.git

git remote
git remote -v


## comando PULL se usa asi:
$ git pull <remote> <branch>

git pull origin master

## if you want to fetch all the information that "origin" has but that
## you don’t yet have in your repository, you can run:
$ git fetch [remote-name]
git fetch origin

## Renombrar
$ git remote rename origin otronombre
$ git remote
## Remover
$ git remote rm origin

git clone https://github.com/ronaldrobles/repoName.git

git add .    #adds all new files
git add -u   #updates tracking for files that changed names or were deleted
git add -A   #does both of the previous 

git commit -m "message"

##git push [remote-name] [branch-name].
git push

#################################    EJEMPLO


***- Clone repository

git clone [url]
#Se crea una carpeta con el nombre del repositorio de github en la carpeta actual(terminal)
#ingresar a la carpeta creada (repositorio)
#anadir o crear archivos
#add new files to local repository

git remote add [shortname] [url]
git remote  #comprobar si la conexion con el [shortname] aparece 

git add *.*
git add .    #adds all new files
git add -u   #updates tracking for files that changed names or were deleted
git add -A   #does both of the previous 

git commit -m "mensaje"

git push <shortname> master
################################################

-Configure push destination  (remote repository)
git remote add <name> https://github.com/ronaldrobles/ExData_plotting1.git

-Push
git push <name>


## Cambiar el mensaje del commit

git commit --amend -m "New commit message"
-and then
git push --force <name> <BRANCH-NAME>






git checkout -b branchname
git branch
git checkout master



Git documentation
          http://git-scm.com/doc
Github help 
          https://help.github.com/





######## SHINYAPPS  ##########

shinyapps::setAccountInfo(
    name="rrobles", 
    token="8F8882AAD490C299507703FAA765A051", 
    secret="<SECRET>")

    secret="gREsHhBuNkyEB26Go7Z8sS0OvPvzUZ3aXAOfadEF")

shinyapps::setAccountInfo(
	name='rrobles', 
	token='8F8882AAD490C299507703FAA765A051', 
	secret='gREsHhBuNkyEB26Go7Z8sS0OvPvzUZ3aXAOfadEF')
